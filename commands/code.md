---
description: Implement a well-scoped code change and verify it. Runs as the cs-coder agent (sonnet). Give it the files and intended behavior; it edits the smallest change that does the job and runs the narrowest check.
argument-hint: [change to make]
context: fork
agent: cs-coder
model: sonnet
---

$ARGUMENTS
