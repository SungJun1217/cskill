---
description: Locate code or answer a where/how question across the repo. Read-only and cheap — runs as the cs-explorer agent (haiku). Returns conclusions, not file dumps.
argument-hint: [what to find / question]
context: fork
agent: cs-explorer
model: haiku
---

$ARGUMENTS
