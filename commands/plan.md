---
description: Turn a non-trivial task into a complete, unambiguous implementation plan. Runs as the cs-planner agent (opus), read-only. Use before coding when the work spans files or has unknowns.
argument-hint: [task to plan]
context: fork
agent: cs-planner
model: opus
---

$ARGUMENTS
